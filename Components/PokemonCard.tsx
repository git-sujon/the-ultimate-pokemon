
const PokemonCard = ( {poke} ) => {
console.log(poke);
    const { url,
        name,
        image
    } = poke
    return (
        <div>
            {/* card design  */}
        </div>
    );
};

export default PokemonCard;